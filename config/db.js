'use strict';

// config/db.js

// DATABASE CONFIGS
// =============================================================================

var DbConfigs = {
  forceSync: true, //force a complete resync of the database on each load
};

module.exports = DbConfigs;
